export const baseURL = 'https://sign-kit-api.herokuapp.com/sign-kit'

// BrightWords backend base for AI (explain, normalize, gloss, chat). When embedded use same origin /api.
export const aiApiBase = process.env.REACT_APP_AI_API_BASE || ''